FROM python:3.11-slim

WORKDIR /app
RUN pip install --no-cache-dir kafka-python==2.0.2 requests==2.32.3

COPY env.py producer.py weather_producer.py ./

# The actual entrypoint is chosen by docker-compose `command:`.
CMD ["python", "producer.py"]
